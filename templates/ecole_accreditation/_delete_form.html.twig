<form method="post" action="{{ path('app_ecole_accreditation_delete', {'id': ecole_accreditation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ecole_accreditation.id) }}">
    <button class="btn">Delete</button>
</form>
