{% extends 'base.html.twig' %}

{% block title %}Edit EcoleAccreditation{% endblock %}

{% block body %}
    <h1>Edit EcoleAccreditation</h1>

    {{ include('ecole_accreditation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ecole_accreditation_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}{{ include('ecole_accreditation/_delete_form.html.twig') }}{% endif %}
{% endblock %}
