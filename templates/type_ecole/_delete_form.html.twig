<form method="post" action="{{ path('app_type_ecole_delete', {'id': type_ecole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_ecole.id) }}">
    <button class="btn">Delete</button>
</form>
