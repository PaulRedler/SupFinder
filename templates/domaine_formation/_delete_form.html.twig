<form method="post" action="{{ path('app_domaine_formation_delete', {'id': domaine_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domaine_formation.id) }}">
    <button class="btn">Delete</button>
</form>
