{% extends 'base.html.twig' %}

{% block title %}Edit Accreditation{% endblock %}

{% block body %}
    <h1>Edit Accreditation</h1>

    {{ include('accreditation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_accreditation_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}{{ include('accreditation/_delete_form.html.twig') }}{% endif %}
{% endblock %}
