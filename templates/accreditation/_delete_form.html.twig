<form method="post" action="{{ path('app_accreditation_delete', {'id': accreditation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accreditation.id) }}">
    <button class="btn">Delete</button>
</form>
