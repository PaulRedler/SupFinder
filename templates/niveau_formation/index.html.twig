{% extends 'base.html.twig' %}

{% block title %}NiveauFormation index{% endblock %}

{% block body %}
    <h1>NiveauFormation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Libelle</th>
                <th>Ordre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for niveau_formation in niveau_formations %}
            <tr>
                <td>{{ niveau_formation.id }}</td>
                <td>{{ niveau_formation.code }}</td>
                <td>{{ niveau_formation.libelle }}</td>
                <td>{{ niveau_formation.ordre }}</td>
                <td>
                    <a href="{{ path('app_niveau_formation_show', {'id': niveau_formation.id}) }}">show</a>
                    <a href="{{ path('app_niveau_formation_edit', {'id': niveau_formation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_niveau_formation_new') }}">Create new</a>
{% endblock %}
