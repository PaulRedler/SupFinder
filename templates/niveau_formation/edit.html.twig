{% extends 'base.html.twig' %}

{% block title %}Edit NiveauFormation{% endblock %}

{% block body %}
    <h1>Edit NiveauFormation</h1>

    {{ include('niveau_formation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_niveau_formation_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}{{ include('niveau_formation/_delete_form.html.twig') }}{% endif %}
{% endblock %}
