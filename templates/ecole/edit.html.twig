{% extends 'base.html.twig' %}

{% block title %}Edit Ecole{% endblock %}

{% block body %}
    <h1>Edit Ecole</h1>

    {{ include('ecole/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ecole_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}{{ include('ecole/_delete_form.html.twig') }}{% endif %}
{% endblock %}
